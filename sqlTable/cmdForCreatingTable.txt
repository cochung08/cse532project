modified at Novemeber 2th:


 
CREATE TABLE ARTICLE2 (
          	ARTICLE_ID INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1),
          	CIT VARCHAR(100),
          	REF VARCHAR(100),
          	AU_CORR VARCHAR(1000),
          	TITLE VARCHAR(1000),
          	JNL_TIT VARCHAR(1000),
          	VOL VARCHAR(1000),
          	ISSUE VARCHAR(1000),
          	YEAR VARCHAR(1000),
          	PG_ST VARCHAR(1000),
          	PG_EN VARCHAR(1000),
          	LANG VARCHAR(300),
          	ABS DBCLOB(100000),
          	PUBID DBCLOB (100000),
          	DOI DBCLOB (100000),
          	URL VARCHAR(1000),
          	PUBTYPE DBCLOB (100000),
          	FUND VARCHAR(1000),
          	RATE VARCHAR(100),
          	SCR VARCHAR(1000),
          	FNLRATE VARCHAR (10),
          	FNLDESIGN VARCHAR (100),
          	MCNRATE VARCHAR (100),
          	MCNCONF VARCHAR (100)
   	)


























































--<ScriptOptions statementTerminator=;/>




CREATE TABLE AUTHOR (
		AU_FULL VARCHAR(100) NOT NULL, 
		ARTICLE_ID INTEGER NOT NULL, 
		AU_SHORT VARCHAR(100)
	)
	DATA CAPTURE NONE;

CREATE TABLE KEYWORD (
		KEYWORD VARCHAR(1000) NOT NULL, 
		ARTICLE_ID INTEGER NOT NULL
	)
	DATA CAPTURE NONE;

ALTER TABLE ARTICLE ADD CONSTRAINT ARTICLE_PK PRIMARY KEY
	(ARTICLE_ID);

ALTER TABLE AUTHOR ADD CONSTRAINT AUTHOR_PK PRIMARY KEY
	(AU_FULL, 
	 ARTICLE_ID);

ALTER TABLE KEYWORD ADD CONSTRAINT KEYWORD_PK PRIMARY KEY
	(KEYWORD, 
	 ARTICLE_ID);

ALTER TABLE AUTHOR ADD CONSTRAINT AUTHOR_ARTICLE_FK FOREIGN KEY
	(ARTICLE_ID)
	REFERENCES ARTICLE
	(ARTICLE_ID);

ALTER TABLE KEYWORD ADD CONSTRAINT KEYWORD_ARTICLE_FK FOREIGN KEY
	(ARTICLE_ID)
	REFERENCES ARTICLE
	(ARTICLE_ID);



